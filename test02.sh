#!/bin/dash
#The perl program recognises the string interpolation when using ' ' when shell doesn't.
# Perl program isn't suppose to recognise it. It should print andrew rather than $name.
name=andrew
echo 'Hello my $name is Tim'